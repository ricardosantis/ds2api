{
  "name": "ds2api-heroku",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "npm install --prefix webui --include=dev && npm run build --prefix webui"
  },
  "engines": {
    "node": "20.x"
  },
  "devDependencies": {}
}